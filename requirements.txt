# Core dependencies
numpy>=1.24.0

# Optional: for reading safetensors checkpoints
safetensors>=0.4.0

# Optional: for psutil in benchmark profiler (falls back gracefully if absent)
psutil>=5.9.0

# Optional: for Metal GPU monitoring on Apple Silicon
# (macOS system tools used as fallback)
# No additional packages required for core functionality